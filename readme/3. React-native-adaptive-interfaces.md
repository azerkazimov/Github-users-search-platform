1. Expo spash icon - https://docs.expo.dev/develop/user-interface/splash-screen-and-app-icon/
   ```npx expo start -c ```
   ```npx expo prebuild```

Aдаптивные интерфейсы и платформенно-зависимый код в React Native** — это ключ к созданию кроссплатформенных мобильных приложений, которые одинаково хорошо работают на iOS и Android.  




---

## 🧩 1. Адаптивные интерфейсы (Responsive UI)

Цель — сделать так, чтобы интерфейс корректно отображался на разных экранах, устройствах и ориентациях.

### 🔹 Основные принципы

#### ✅ 1. Использование Flexbox

React Native использует **Flexbox** по умолчанию для адаптивных макетов:

```js
<View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
  <Text>Hello World</Text>
</View>
```

Flexbox автоматически подстраивается под размеры экрана, обеспечивая адаптивность.

#### ✅ 2. Единицы измерения: `Dimensions` и `useWindowDimensions`

Позволяют вычислить ширину и высоту экрана:

```js
import { useWindowDimensions } from 'react-native';

const Example = () => {
  const { width, height } = useWindowDimensions();

  return (
    <View style={{ width: width * 0.9, height: height * 0.5, backgroundColor: 'skyblue' }} />
  );
};
```

#### ✅ 3. Процентные значения и `aspectRatio`

Можно задавать относительные размеры и соотношения сторон:

```js
<View style={{ width: '80%', aspectRatio: 1, backgroundColor: 'tomato' }} />
```

#### ✅ 4. Использование `SafeAreaView`

На iPhone X и выше экран имеет "чёлку" и закруглённые углы — `SafeAreaView` предотвращает наложение элементов на системные зоны:

```js
import { SafeAreaView } from 'react-native';

<SafeAreaView style={{ flex: 1 }}>
  <Text>Контент в безопасной зоне</Text>
</SafeAreaView>
```

#### ✅ 5. Поддержка ориентации экрана

```js
import { useEffect } from 'react';
import { Dimensions } from 'react-native';

useEffect(() => {
  const subscription = Dimensions.addEventListener('change', ({ window }) => {
    console.log('Orientation changed:', window);
  });
  return () => subscription?.remove();
}, []);
```

---

## ⚙️ 2. Платформенно-зависимый код

Иногда нужно использовать разные стили или функционал для **Android** и **iOS**.

### 🔹 1. Использование `Platform` API

```js
import { Platform, StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    backgroundColor: Platform.OS === 'ios' ? 'blue' : 'green',
  },
});
```

Или для логики:

```js
if (Platform.OS === 'android') {
  console.log('Running on Android');
}
```

---

### 🔹 2. Платформенно-зависимые файлы

React Native автоматически выбирает нужный файл, если указать суффикс `.ios.js` или `.android.js`.

Например:

```
Button.ios.js
Button.android.js
```

Импорт будет одинаков:

```js
import Button from './Button';
```

RN сам подберёт нужный файл под платформу.

---

### 🔹 3. Компоненты с платформенными отличиями

Некоторые компоненты ведут себя по-разному:

|Компонент|Отличие|
|---|---|
|`TouchableOpacity` / `TouchableNativeFeedback`|`TouchableNativeFeedback` доступен только на Android|
|`DatePickerIOS` / `DatePickerAndroid`|Разделены, можно использовать через `@react-native-community/datetimepicker`|
|`StatusBar`|Имеет разные параметры для iOS и Android|

Пример:

```js
import { Platform, StatusBar } from 'react-native';

<StatusBar
  barStyle={Platform.OS === 'ios' ? 'dark-content' : 'light-content'}
/>
```

---

## 🧠 3. Библиотеки для адаптивного дизайна

|Библиотека|Описание|
|---|---|
|**react-native-responsive-screen**|Проценты от экрана (`wp`, `hp`)|
|**react-native-size-matters**|Масштабирование размеров относительно экрана|
|**react-native-responsive-dimensions**|Простая адаптация размеров для разных устройств|
|**react-native-safe-area-context**|Продвинутая работа с безопасными зонами|

Пример с `react-native-responsive-screen`:

```js
import { widthPercentageToDP as wp, heightPercentageToDP as hp } from 'react-native-responsive-screen';

<View style={{ width: wp('90%'), height: hp('20%'), backgroundColor: 'orange' }} />
```

---

## 💡 4. Лучшие практики

- Используй **Flexbox**, а не фиксированные размеры.
    
- Всегда тестируй интерфейс на **разных устройствах и ориентациях**.
    
- Для сложных макетов используй **UI-библиотеки**:
    
    - `React Native Paper`
        
    - `NativeBase`
        
    - `React Native Elements`
        
- Избегай «магических чисел» — размеры лучше рассчитывать динамически.
    

---

## 🚀  Expo автоматически управляет Splash и Icon через `app.json` (или `app.config.js`)

Все настройки происходят в конфигурационном файле проекта.

---

## 📱 2. Настройка **App Icon**

### 🔹 Подготовка иконки

- Размер: **1024×1024 px**
    
- Формат: **.png**
    
- Без прозрачности (иначе могут быть артефакты на Android)
    
- Хранить, например, в папке `assets/icon.png`
    

### 🔹 Пропиши в `app.json`:

```json
{
  "expo": {
    "name": "MyApp",
    "slug": "myapp",
    "icon": "./assets/icon.png",
    "orientation": "portrait"
  }
}
```

Expo сам создаст нужные версии иконок для Android и iOS при сборке.  
Если хочешь кастомно задать — можно через `adaptiveIcon`.

#### Для Android adaptive icon:

```json
"android": {
  "adaptiveIcon": {
    "foregroundImage": "./assets/adaptive-icon.png",
    "backgroundColor": "#FFFFFF"
  }
}
```

---

## 💦 3. Настройка **Splash Screen**

### 🔹 Подготовка изображения

- Формат: `.png`
    
- Размер: **зависит от экрана**, но Expo подгоняет автоматически
    
- Лучше сделать квадратную картинку (примерно 1242×1242 px)
    
- Путь, например: `./assets/splash.png`
    

---

### 🔹 Пример конфигурации splash:

```json
{
  "expo": {
    "splash": {
      "image": "./assets/splash.png",
      "resizeMode": "contain",
      "backgroundColor": "#ffffff"
    }
  }
}
```

#### Параметры:

|Параметр|Описание|
|---|---|
|`image`|Путь к изображению splash|
|`resizeMode`|`"contain"` или `"cover"` — как подгоняется картинка|
|`backgroundColor`|Цвет фона|

🔸 **Рекомендация:**

- `contain` — если хочешь, чтобы логотип был виден полностью
    
- `cover` — если хочешь, чтобы splash полностью заполнял экран
    

---

## ⚙️ 4. Отдельные настройки для платформ

Можно указать разные splash для Android и iOS:

```json
{
  "expo": {
    "splash": {
      "image": "./assets/splash.png",
      "resizeMode": "contain",
      "backgroundColor": "#ffffff"
    },
    "android": {
      "splash": {
        "image": "./assets/android-splash.png",
        "resizeMode": "cover",
        "backgroundColor": "#000000"
      }
    },
    "ios": {
      "splash": {
        "image": "./assets/ios-splash.png",
        "resizeMode": "contain",
        "backgroundColor": "#ffffff"
      }
    }
  }
}
```

---

## 🧠 5. Кастомизация (через библиотеку `expo-splash-screen`)

Если хочешь вручную контролировать момент, когда Splash исчезает:

```bash
npx expo install expo-splash-screen
```

### Использование:

```js
import * as SplashScreen from 'expo-splash-screen';
import { useEffect } from 'react';

SplashScreen.preventAutoHideAsync(); // не скрывать автоматически

export default function App() {
  useEffect(() => {
    const prepare = async () => {
      // Подгрузка данных, шрифтов и т.п.
      await new Promise(resolve => setTimeout(resolve, 2000));
      await SplashScreen.hideAsync(); // скрыть splash вручную
    };
    prepare();
  }, []);

  return <Text>App Loaded</Text>;
}
```

---

## 🧰 6. Проверка и тестирование

Запусти проект:

```bash
npx expo start
```

Для проверки иконки и splash в сборке:

```bash
npx expo prebuild
npx expo run:android
npx expo run:ios
```

---

## ✅ Итого

|Что|Где настраивается|Пример|
|---|---|---|
|App Icon|`expo.icon`|`"./assets/icon.png"`|
|Android Adaptive Icon|`expo.android.adaptiveIcon`|foreground + background|
|Splash Screen|`expo.splash`|`"./assets/splash.png"`|
|Раздельные splash|`expo.android.splash`, `expo.ios.splash`|разные изображения|

---

Подключение пользовательских шрифтов в React Native с Expo** — это один из самых распространённых и полезных этапов при кастомизации интерфейса.  

---

## 🧩 1. Добавь шрифты в проект

Создай папку (если её нет):

```
/assets/fonts
```

Положи туда файлы шрифтов, например:

```
/assets/fonts/
  Montserrat-Regular.ttf
  Montserrat-Bold.ttf
```

---

## ⚙️ 2. Обнови `app.json` (или `app.config.js`)

Чтобы Expo знал, что нужно включить эти файлы в сборку:

```json
{
  "expo": {
    "assetBundlePatterns": [
      "**/*"
    ]
  }
}
```

_(Эта строка обычно уже есть по умолчанию — просто убедись, что она присутствует.)_

---

## 📦 3. Установи библиотеку Expo Fonts

В Expo уже есть готовая библиотека:

```bash
npx expo install expo-font
```

---

## 🪄 4. Загрузка шрифтов в коде

### Пример с хуком `useFonts` (современный способ)

```js
import React from 'react';
import { Text, View } from 'react-native';
import { useFonts } from 'expo-font';

export default function App() {
  const [fontsLoaded] = useFonts({
    'Montserrat-Regular': require('./assets/fonts/Montserrat-Regular.ttf'),
    'Montserrat-Bold': require('./assets/fonts/Montserrat-Bold.ttf'),
  });

  if (!fontsLoaded) {
    return null; // можно вернуть SplashScreen или Loader
  }

  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontFamily: 'Montserrat-Regular', fontSize: 20 }}>
        Regular Font
      </Text>
      <Text style={{ fontFamily: 'Montserrat-Bold', fontSize: 20 }}>
        Bold Font
      </Text>
    </View>
  );
}
```

---

## ⏳ 5. Совместное использование со Splash Screen

Если хочешь **не показывать экран, пока шрифты не загрузятся**, можно использовать `expo-splash-screen`:

```bash
npx expo install expo-splash-screen
```

И в коде:

```js
import React, { useCallback, useEffect } from 'react';
import { Text, View } from 'react-native';
import { useFonts } from 'expo-font';
import * as SplashScreen from 'expo-splash-screen';

SplashScreen.preventAutoHideAsync();

export default function App() {
  const [fontsLoaded] = useFonts({
    'Montserrat-Regular': require('./assets/fonts/Montserrat-Regular.ttf'),
    'Montserrat-Bold': require('./assets/fonts/Montserrat-Bold.ttf'),
  });

  const onLayoutRootView = useCallback(async () => {
    if (fontsLoaded) {
      await SplashScreen.hideAsync();
    }
  }, [fontsLoaded]);

  if (!fontsLoaded) {
    return null;
  }

  return (
    <View onLayout={onLayoutRootView} style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontFamily: 'Montserrat-Regular', fontSize: 20 }}>Hello</Text>
      <Text style={{ fontFamily: 'Montserrat-Bold', fontSize: 20 }}>Expo Fonts Loaded</Text>
    </View>
  );
}
```

---

## 🧠 6. Использование Google Fonts (альтернатива)

Expo также предоставляет библиотеку для загрузки шрифтов напрямую из Google Fonts:

```bash
npx expo install @expo-google-fonts/montserrat expo-font
```

Затем просто импортируй нужный набор:

```js
import { useFonts, Montserrat_400Regular, Montserrat_700Bold } from '@expo-google-fonts/montserrat';

const [fontsLoaded] = useFonts({
  Montserrat_400Regular,
  Montserrat_700Bold,
});
```

И используешь:

```js
<Text style={{ fontFamily: 'Montserrat_700Bold' }}>Google Font Loaded</Text>
```

---

## ✅ ИТОГ

|Этап|Действие|
|---|---|
|1|Помести файлы шрифтов в `/assets/fonts`|
|2|Убедись, что `"assetBundlePatterns": ["**/*"]` есть в `app.json`|
|3|Установи `expo-font`|
|4|Подключи шрифты через `useFonts`|
|5|(Опционально) совмести со `expo-splash-screen`|
|6|(Альтернатива) Используй шрифты Google через `@expo-google-fonts`|

---
Как добавить навигацию с вкладками (Tabs) в **React Native с Expo**.  
Это одна из самых часто используемых частей интерфейса, особенно в мобильных приложениях.

---

## 🧩 1. Установка навигации

Expo использует библиотеку **React Navigation**, в которой вкладки реализованы через `@react-navigation/bottom-tabs`.

Выполни установку:

```bash
npx expo install @react-navigation/native
npx expo install react-native-screens react-native-safe-area-context
npx expo install @react-navigation/bottom-tabs
```

_(Если у тебя ещё нет Stack навигации, можно позже добавить её тоже.)_

---

## ⚙️ 2. Базовая настройка навигации

Создай структуру проекта:

```
/App.js
/navigation/
  TabNavigator.js
/screens/
  HomeScreen.js
  SettingsScreen.js
  ProfileScreen.js
```

---

## 📁 3. Пример `App.js`

```js
import * as React from 'react';
import { NavigationContainer } from '@react-navigation/native';
import TabNavigator from './navigation/TabNavigator';

export default function App() {
  return (
    <NavigationContainer>
      <TabNavigator />
    </NavigationContainer>
  );
}
```

---

## 🧭 4. Файл `navigation/TabNavigator.js`

```js
import React from 'react';
import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';
import { Ionicons } from '@expo/vector-icons'; // иконки из Expo
import HomeScreen from '../screens/HomeScreen';
import SettingsScreen from '../screens/SettingsScreen';
import ProfileScreen from '../screens/ProfileScreen';

const Tab = createBottomTabNavigator();

export default function TabNavigator() {
  return (
    <Tab.Navigator
      screenOptions={({ route }) => ({
        headerShown: false, // скрыть верхний хедер
        tabBarShowLabel: true, // подписи под иконками
        tabBarActiveTintColor: '#1251A3',
        tabBarInactiveTintColor: 'gray',
        tabBarStyle: {
          backgroundColor: '#fff',
          borderTopWidth: 0,
          elevation: 5,
          height: 60,
          paddingBottom: 5,
        },
        tabBarIcon: ({ color, size }) => {
          let iconName;
          if (route.name === 'Home') iconName = 'home';
          else if (route.name === 'Settings') iconName = 'settings';
          else if (route.name === 'Profile') iconName = 'person';

          return <Ionicons name={iconName} size={size} color={color} />;
        },
      })}
    >
      <Tab.Screen name="Home" component={HomeScreen} />
      <Tab.Screen name="Settings" component={SettingsScreen} />
      <Tab.Screen name="Profile" component={ProfileScreen} />
    </Tab.Navigator>
  );
}
```

---

## 🧩 5. Примеры экранов

### `screens/HomeScreen.js`

```js
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export default function HomeScreen() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>🏠 Home Screen</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', alignItems: 'center' },
  text: { fontSize: 20, fontWeight: '600' },
});
```

Аналогично создай `SettingsScreen.js` и `ProfileScreen.js`.

---

## 🎨 6. Кастомизация TabBar

Ты можешь менять цвет, высоту, фон, добавлять иконки, бэйджи и анимации.

Пример с **кастомным фоном и без подписей**:

```js
<Tab.Navigator
  screenOptions={{
    headerShown: false,
    tabBarShowLabel: false,
    tabBarStyle: {
      backgroundColor: '#1251A3',
      borderTopWidth: 0,
      height: 70,
    },
  }}
>
```

---

## 🔁 7. Комбинация Tabs + Stack

Очень часто используют **Stack внутри Tabs**, чтобы на каждой вкладке была своя стек-навигация:

```
NavigationContainer
 └── TabNavigator
      ├── HomeStack
      ├── ProfileStack
      └── SettingsStack
```

Для этого:

```bash
npx expo install @react-navigation/native-stack
```

и создаёшь, например, `HomeStack.js`:

```js
import { createNativeStackNavigator } from '@react-navigation/native-stack';
import HomeScreen from '../screens/HomeScreen';
import DetailsScreen from '../screens/DetailsScreen';

const Stack = createNativeStackNavigator();

export default function HomeStack() {
  return (
    <Stack.Navigator>
      <Stack.Screen name="Home" component={HomeScreen} />
      <Stack.Screen name="Details" component={DetailsScreen} />
    </Stack.Navigator>
  );
}
```

---

## ✅ ИТОГ

|Этап|Действие|
|---|---|
|1|Установить `@react-navigation/native` и `@react-navigation/bottom-tabs`|
|2|Обернуть приложение в `NavigationContainer`|
|3|Создать `Tab.Navigator`|
|4|Добавить экраны (Home, Profile, Settings)|
|5|(Опционально) подключить `Ionicons` для иконок|
|6|Кастомизировать TabBar под дизайн приложения|

---
# **`KeyboardAvoidingView`** 

В React Native используется для того, чтобы элементы интерфейса (особенно поля ввода) **не перекрывались клавиатурой**.  
Это одна из самых нужных вещей при создании форм, логинов, чатов и т.п.

---

## 🎯 Цель

Когда открывается клавиатура (особенно на iOS), она может перекрыть `TextInput`.  
`KeyboardAvoidingView` "поднимает" контент, чтобы поля оставались видимыми.

---

## ⚙️ 1. Простейший пример

```js
import React, { useState } from 'react';
import { View, TextInput, Button, KeyboardAvoidingView, Platform, StyleSheet } from 'react-native';

export default function App() {
  const [text, setText] = useState('');

  return (
    <KeyboardAvoidingView
      behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
      style={styles.container}
    >
      <View style={styles.inner}>
        <TextInput
          placeholder="Type something..."
          style={styles.input}
          value={text}
          onChangeText={setText}
        />
        <Button title="Send" onPress={() => console.log(text)} />
      </View>
    </KeyboardAvoidingView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  inner: {
    flex: 1,
    justifyContent: 'center',
    padding: 24,
  },
  input: {
    height: 50,
    borderColor: '#ccc',
    borderWidth: 1,
    borderRadius: 8,
    paddingHorizontal: 10,
    marginBottom: 10,
  },
});
```

### 💡 Что здесь важно:

- `behavior` определяет, **как** экран будет сдвигаться при появлении клавиатуры.
    
- Для iOS чаще всего используют `"padding"`.
    
- Для Android — `"height"` или `"position"`.
    

---

## 🧠 2. Поведение `behavior`

|Значение|Описание|
|---|---|
|`"height"`|Изменяет высоту контейнера|
|`"position"`|Сдвигает содержимое вверх|
|`"padding"`|Добавляет отступ внизу, равный высоте клавиатуры|

🔹 _Рекомендация:_  
`padding` — лучший вариант для iOS,  
`height` — для Android.

---

## 🧩 3. Использование с `ScrollView`

Если у тебя форма с множеством полей — используй вместе с `ScrollView`:

```js
import { KeyboardAvoidingView, ScrollView, Platform, TextInput } from 'react-native';

<KeyboardAvoidingView
  behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
  style={{ flex: 1 }}
>
  <ScrollView contentContainerStyle={{ flexGrow: 1, padding: 16 }}>
    <TextInput placeholder="Name" style={styles.input} />
    <TextInput placeholder="Email" style={styles.input} />
    <TextInput placeholder="Password" style={styles.input} secureTextEntry />
  </ScrollView>
</KeyboardAvoidingView>
```

⚠️ _Важно:_ добавляй `contentContainerStyle={{ flexGrow: 1 }}`  
иначе `ScrollView` может не растянуться на всю высоту.

---

## 🔐 4. Если нужно скрывать клавиатуру при клике вне поля

Добавь `TouchableWithoutFeedback` с `Keyboard.dismiss`:

```js
import { Keyboard, TouchableWithoutFeedback } from 'react-native';

<KeyboardAvoidingView
  behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
  style={{ flex: 1 }}
>
  <TouchableWithoutFeedback onPress={Keyboard.dismiss}>
    <View style={{ flex: 1, justifyContent: 'center', padding: 16 }}>
      <TextInput placeholder="Type here" style={styles.input} />
    </View>
  </TouchableWithoutFeedback>
</KeyboardAvoidingView>
```

Теперь клавиатура закроется при нажатии на пустое место.

---

## 🧰 5. Сочетание с SafeAreaView

На iPhone с «чёлкой» (`notch`) лучше оборачивать всё в `SafeAreaView`:

```js
import { SafeAreaView } from 'react-native-safe-area-context';

<SafeAreaView style={{ flex: 1 }}>
  <KeyboardAvoidingView
    behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
    style={{ flex: 1 }}
  >
    {/* Контент */}
  </KeyboardAvoidingView>
</SafeAreaView>
```

---

## ✅ ИТОГ

|Что делает|Как использовать|
|---|---|
|Избегает перекрытия клавиатурой|`<KeyboardAvoidingView behavior="padding" />`|
|Работает по-разному на iOS и Android|`Platform.OS === 'ios' ? 'padding' : 'height'`|
|Совместим с `ScrollView`|Да, особенно для длинных форм|
|Можно закрывать клавиатуру кликом|`TouchableWithoutFeedback onPress={Keyboard.dismiss}`|

---
